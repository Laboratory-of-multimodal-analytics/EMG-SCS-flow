# Compatible ranges. For a working install use requirements-lock.txt (exact tested
# versions) in its own environment -- see README, "Installing".
# Upper bounds stop the next major release (not tested) from arriving by itself.
# Checked with both the locked set and the newest releases of September 2026
# (numpy 2.5, pandas 3.0, matplotlib 3.11, mne 1.13, PySide6 6.11).

mne>=1.5.0,<2
numpy>=1.22,<3
scipy>=1.9,<2
pandas>=1.4,<4
matplotlib>=3.5,<4
tqdm>=4.64
xlsxwriter>=3.0

# Interactive toolbox (python -m emgflow); not needed for the scripted pipeline
PySide6>=6.5,<7
